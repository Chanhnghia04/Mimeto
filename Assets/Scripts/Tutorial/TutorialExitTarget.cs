using UnityEngine;

/// <summary>
/// Cửa thoát mẫu trong guided gameplay course.
/// </summary>
public sealed class TutorialExitTarget : MonoBehaviour
{
    public string exitHint = "Nhấn E để thoát";
    public Color accentColor = new Color(0.05f, 0.9f, 1f);
}
